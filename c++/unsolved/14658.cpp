#include<iostream>
#include<unordered_map>
#include<vector>
#include<algorithm>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int limit_x, limit_y, length, stars, max = 0;
    cin >> limit_x >> limit_y >> length >> stars;
    
    length++;
    int x = 1, y = 1, start_x = 0;

    for(int i = 0; i < stars; i++){
        int ix, iy;
        cin >> ix >> iy;
        
        
    }

    return 0;
}