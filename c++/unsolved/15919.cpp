#include<iostream>
#include<vector>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    pair<int, int> dp[1001];
    int n, m;
    cin >> n >> m;

    

    return 0;
}