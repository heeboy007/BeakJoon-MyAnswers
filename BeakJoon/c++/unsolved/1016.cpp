#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;

vector<int> primes = {2, 3, 5 ,7};

int main() {
    
	long long n, m;
	cin >> n >> m;
	
	int until = (int)floor(sqrt(m));
	for(int i = 11; i <= until; i += 2){
	    
	}
	
	return 0;
}
